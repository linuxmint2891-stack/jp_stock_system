name: Daily Scrape
on:
  workflow_dispatch: # これが「ボタン」の正体です

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run
        run: cargo run --release
